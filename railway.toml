[build]
builder = "nixpacks"
buildCommand = "yarn install && yarn prisma:generate && yarn build"

[deploy]
startCommand = "yarn prisma:migrate && yarn start:prod"
healthcheckPath = "/api"
healthcheckTimeout = 100